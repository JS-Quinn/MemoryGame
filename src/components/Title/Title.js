import React from "react";
import "./Title.css"

const Title = props => (
    <div className="title-section">
        <h1>Bob's Memory Game</h1>
        <p>Click on an image to earn points, but don't click on an image any more than once!</p>
    </div>
);

export default Title;